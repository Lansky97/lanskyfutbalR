% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_match_impact_table.R
\name{generate_match_impact_table}
\alias{generate_match_impact_table}
\title{Generate Match Impact Table}
\usage{
generate_match_impact_table(projection, game_id, race_teams, race_place)
}
\arguments{
\item{projection}{A data frame containing the projection data.}

\item{game_id}{An integer specifying the game ID.}

\item{race_teams}{A character vector specifying the teams involved in the race.}

\item{race_place}{A character string specifying the race place category (e.g., "champion", "UCL", "UEFA", "REL").}
}
\value{
A \code{gt} table object displaying the match impact.
}
\description{
This function generates a formatted table showing the impact of a match on various race outcomes (e.g., league championship, Champions League qualification, etc.).
}
\details{
The function calculates the match impact based on the projection data and formats the results into a \code{gt} table.
It highlights the maximum values in the numeric columns and includes a header with the match details and a footnote.

The subtitle of the table provides information on the race place category:
\itemize{
\item "champion": Odds to win the league given result.
\item "UCL": Odds to make Champions League given result.
\item "UEFA": Odds to make Europe given result.
\item "REL": Odds to be relegated given result.
}

The table is styled with \code{gt::tab_style()} to highlight the highest values in each numeric column with a light green background.
}
\examples{
\dontrun{
projection_data <- your_projection_data_frame
game_id <- 123
race_teams <- c("TeamA", "TeamB")
race_place <- "champion"

generate_match_impact_table(projection_data, game_id, race_teams, race_place)
}
}
